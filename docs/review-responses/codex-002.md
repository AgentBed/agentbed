# Response to review codex-002 (2026-08-23, Revision 2 working tree vs b4abb04)

Verdict accepted: **NEEDS REVISION** → this working tree is now **Revision 3**. All eight findings are accepted — none rebutted this round — and all are engineering-contract fixes, which matches the reviewer's closing note that no further owner/product decision was needed. Blocking-question closures are acknowledged with thanks; the shared-desktop condition (best-effort attribution never used for security authorization or non-repudiation) is adopted verbatim into §6.5's trust statement scope.

| Finding | Disposition in Revision 3 |
|---|---|
| Watchdog has no atomic commit transition (Critical) | **Accepted.** effects.md §3a adds `PROBATION_PASSED → COMMITTING → COMMITTED`, per-arming epochs/fencing tokens, a time-bounded commit lease with the watchdog **staying armed through `switch`**, a single CAS-decided winner between `BEGIN_REVERT`/`BEGIN_COMMIT`, durable boot-default verification + WAL fsync before disarm, and the rule that every interruption before durable verification is uncommitted (recovery re-verifies the actual boot default). The slow-switch race is closed by the lease fencing out deadline-revert after `BEGIN_COMMIT` wins. |
| Class E has no crash/idempotency contract (Critical) | **Accepted.** effects.md §3b: `APPROVED → DISPATCHING → SUCCEEDED | FAILED | OUTCOME_UNKNOWN`; approval consumed durably before dispatch; provider idempotency keys + persisted provider operation ids; `OUTCOME_UNKNOWN` never auto-retried, provider-specific reconciliation before a human-ordered retry (a new E step); mixed D/M/E tasks explicitly non-atomic. |
| Watchdog as reboot-DoS (High) | **Accepted.** effects.md §4: immutable mandatory invariants separated from manifest checks (additive-only); authenticated out-of-guest MCP round-trip replaces "Tailscale peer visible"; in-guest watchdog is the single recovery coordinator with the OOB observer as evidence-provider + unresponsive-guest backstop; one automatic revert per transaction, one OOB reset per epoch; owner-recovery safe mode after N consecutive failures; propose-time semantic rejection of diffs touching the watchdog closure/unit/state/WAL/rollback path; watchdog binary/config pinned outside the candidate closure. Honest limit (root-vs-root) added to the threat model. |
| Connector caller identity undefined (High) | **Accepted.** ADR §5 connector contract: identity derived server-side from the authenticated MCP identity; no raw socket paths, FDs, or transferable bearer handles — `secret.use` returns an opaque session-bound reference rejected from any other identity; direct executor↔connector paths require stable per-identity UIDs, `0700` runtime dirs, `SO_PEERCRED` verification; FD-passing and cross-identity replay are Gate 3 exit tests (roadmap updated). |
| Origin/path/method scoping escapable (High) | **Accepted.** ADR §5: canonical origin (scheme + normalized host + explicit port); path validation after percent-decoding + dot-segment removal; caller authority/proxy/forwarding/credential/hop-by-hop headers rejected; redirects off by default, re-authorized per hop, credentials never cross-origin; provider-schema-shaped requests; pinned resolution + address-class blocking (loopback/link-local/private/tailnet/multicast/Unix) applied to **both** connectors and the plain proxy; no wildcard credential origins in v0; bounded, credential-redacted responses; every credential-bearing invocation is class E. |
| Audit can't support broker-tamper claim (High) | **Accepted.** ADR §5.2: git push demoted to non-qualifying mirror; anchor must be WORM with compliance-mode retention and a write-only credential (no delete/retention-change/governance-bypass); anchoring key outside the broker (OOB controller/TPM/minimal service); offline behaviour defined (`COMMITTED_UNANCHORED`, further D/M refused); claim narrowed explicitly — post-anchor tampering detectable, pre-anchor honesty not provable; restated in the threat model. |
| Effect/safety contradictions (High) | **Accepted.** effects.md is declared the sole vocabulary source. Effect **sets** computed per call from tool+arguments+manifest, highest class governs (resolves `plugin.install` {D,M}, `shell.exec` {M}+E); refusal rule uniform for D **and** M with per-resource minimums and **no `none` opt-in** (ADR §5.3 contradiction removed; workspaces live on `dedicated_snapshot` volumes); E pre-authorization unified across effects.md/goals.md ("approval **or** explicit scoped pre-authorization"); safety enums totally ordered per resource kind, `dedicated_snapshot` defined for data resources; scalar remnants replaced (ADR §4); schema conformance examples committed as a deliverable (`schemas/examples/`). |
| Shared Podman user contradicts plugin isolation; package globs (High) | **Accepted.** One service user **per plugin trust domain** (own subuid/subgid, storage, runtime dir); cross-plugin control/volume-read tests added to Gate 4; `"python3-*"` example removed — exact names + repo + version/range + resolved digest, no globs. |
| Rev-1 terminology remnants (Medium) | **Accepted.** Sweep done: README "one privileged daemon" replaced; REVIEW.md challenge list updated (CA/TLS item replaced with the connector contract); `min_host_safety` → per-resource `min_safety`; Phase→Gate throughout ADR; Gate 1 evidence now names the WAL journal with the anchored ledger landing at Gate 2; prior-art hypothesis caveat propagated to README. |

Standing request to the reviewer for round 3: verify the atomic commit protocol (effects.md §3a) against your own failure sequence step by step, and attack the connector contract's canonicalization list for anything still missing (e.g. IDN/homoglyph origins, HTTP/2 pseudo-header smuggling, chunked-encoding tricks) — those are the two contracts most likely to still hide a flaw.
